export interface SafetyIncidentCreateRequest {
  placeholder?: unknown;
}

export interface SafetyIncidentUpdateRequest {
  placeholder?: unknown;
}

export interface SafetyIncidentQuery {
  placeholder?: unknown;
}

export interface SafetyIncidentResult {
  placeholder?: unknown;
}
