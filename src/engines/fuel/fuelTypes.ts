export interface FuelEntryCreateRequest {
  placeholder?: unknown;
}

export interface FuelEntryUpdateRequest {
  placeholder?: unknown;
}

export interface FuelEntryQuery {
  placeholder?: unknown;
}

export interface FuelEntryResult {
  placeholder?: unknown;
}
