/**
 * AIMI Engine — Telematics Delivery
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum TELEMATICS_DELIVERY_TYPE {}

export interface TelematicsDeliveryCreateRequest {
  placeholder?: unknown;
}

export interface TelematicsDeliveryUpdateRequest {
  placeholder?: unknown;
}

export interface TelematicsDeliveryQuery {
  placeholder?: unknown;
}

export interface TelematicsDeliveryResult {
  placeholder?: unknown;
}

export class TelematicsDeliveryTypesPlaceholder {}
