/**
 * AIMI Engine — PM
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum PM_REPOSITORY {}

export interface PmRepositoryPlaceholder {
  placeholder?: unknown;
}

export class PmRepository {}
