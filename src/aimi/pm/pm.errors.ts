/**
 * AIMI Engine — PM
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum PM_ERROR {}

export interface PmError {
  placeholder?: unknown;
}

export class PmErrorPlaceholder {}
