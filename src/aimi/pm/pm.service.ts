/**
 * AIMI Engine — PM
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum PM_SERVICE {}

export interface PmServicePlaceholder {
  placeholder?: unknown;
}

export class PmService {}
