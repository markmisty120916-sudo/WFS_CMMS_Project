/**
 * AIMI Engine — Scheduling
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum SCHEDULING_ERROR {}

export interface SchedulingError {
  placeholder?: unknown;
}

export class SchedulingErrorPlaceholder {}
