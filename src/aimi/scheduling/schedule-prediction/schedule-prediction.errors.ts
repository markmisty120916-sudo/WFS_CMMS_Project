/**
 * AIMI Engine — Schedule Prediction
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface SchedulePredictionErrorPlaceholder {
  placeholder?: unknown;
}

export class SchedulePredictionError {}
