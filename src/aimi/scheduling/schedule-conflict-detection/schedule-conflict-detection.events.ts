/**
 * AIMI Engine — Schedule Conflict Detection
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ScheduleConflictDetectionEventsPlaceholder {
  placeholder?: unknown;
}

export class ScheduleConflictDetectionEvents {}
