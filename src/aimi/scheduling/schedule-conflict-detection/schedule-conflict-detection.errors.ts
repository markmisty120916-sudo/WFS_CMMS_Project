/**
 * AIMI Engine — Schedule Conflict Detection
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ScheduleConflictDetectionErrorPlaceholder {
  placeholder?: unknown;
}

export class ScheduleConflictDetectionError {}
