/**
 * AIMI Engine — Schedule Conflict Detection
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ScheduleConflictDetectionActionsPlaceholder {
  placeholder?: unknown;
}

export class ScheduleConflictDetectionActions {}
