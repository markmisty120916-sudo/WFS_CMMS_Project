/**
 * AIMI Engine — Schedule Routing
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ScheduleRoutingTypesPlaceholder {
  placeholder?: unknown;
}

export class ScheduleRoutingTypes {}
