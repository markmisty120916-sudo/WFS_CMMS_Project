/**
 * AIMI Engine — Schedule Routing
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ScheduleRoutingWeightsPlaceholder {
  placeholder?: unknown;
}

export class ScheduleRoutingWeights {}
