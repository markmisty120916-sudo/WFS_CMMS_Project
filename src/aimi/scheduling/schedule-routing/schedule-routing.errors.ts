/**
 * AIMI Engine — Schedule Routing
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ScheduleRoutingErrorPlaceholder {
  placeholder?: unknown;
}

export class ScheduleRoutingError {}
