/**
 * AIMI Engine — Schedule Aggregation
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ScheduleAggregationErrorPlaceholder {
  placeholder?: unknown;
}

export class ScheduleAggregationError {}
