/**
 * AIMI Engine — Schedule Aggregation
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ScheduleAggregationActionsPlaceholder {
  placeholder?: unknown;
}

export class ScheduleAggregationActions {}
