/**
 * AIMI Engine — Schedule Aggregation
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ScheduleAggregationRulesPlaceholder {
  placeholder?: unknown;
}

export class ScheduleAggregationRules {}
