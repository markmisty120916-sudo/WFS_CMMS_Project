/**
 * AIMI Engine — Schedule Normalization
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ScheduleNormalizationWeightsPlaceholder {
  placeholder?: unknown;
}

export class ScheduleNormalizationWeights {}
