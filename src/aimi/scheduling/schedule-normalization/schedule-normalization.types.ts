/**
 * AIMI Engine — Schedule Normalization
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ScheduleNormalizationTypesPlaceholder {
  placeholder?: unknown;
}

export class ScheduleNormalizationTypes {}
