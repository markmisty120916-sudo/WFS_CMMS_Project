/**
 * AIMI Engine — Schedule Ingestion
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ScheduleIngestionWeightsPlaceholder {
  placeholder?: unknown;
}

export class ScheduleIngestionWeights {}
