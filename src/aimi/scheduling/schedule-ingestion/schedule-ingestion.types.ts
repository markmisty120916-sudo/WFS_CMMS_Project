/**
 * AIMI Engine — Schedule Ingestion
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ScheduleIngestionTypesPlaceholder {
  placeholder?: unknown;
}

export class ScheduleIngestionTypes {}
