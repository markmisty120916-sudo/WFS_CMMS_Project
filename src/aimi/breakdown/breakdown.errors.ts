/**
 * AIMI Engine — Breakdown
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum BREAKDOWN_ERROR {}

export interface BreakdownError {
  placeholder?: unknown;
}

export class BreakdownErrorPlaceholder {}
