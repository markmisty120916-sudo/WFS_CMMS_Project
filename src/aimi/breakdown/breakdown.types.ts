/**
 * AIMI Engine — Breakdown
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum BREAKDOWN_TYPE {}

export interface BreakdownCreateRequest {
  placeholder?: unknown;
}

export interface BreakdownUpdateRequest {
  placeholder?: unknown;
}

export interface BreakdownQuery {
  placeholder?: unknown;
}

export interface BreakdownResult {
  placeholder?: unknown;
}

export class BreakdownTypesPlaceholder {}
