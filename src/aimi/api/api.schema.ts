/**
 * AIMI Engine — API
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum API_SCHEMA {}

export interface ApiSchema {
  placeholder?: unknown;
}

export class ApiSchemaPlaceholder {}

export const apiSchema = {};
