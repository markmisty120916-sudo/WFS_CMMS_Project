/**
 * AIMI Engine — API
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum API_ROUTE {}

export interface ApiRoute {
  placeholder?: unknown;
}

export class ApiRoutesPlaceholder {}

export const apiRoutes = {};
