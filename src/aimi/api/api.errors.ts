/**
 * AIMI Engine — API
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum API_ERROR {}

export interface ApiError {
  placeholder?: unknown;
}

export class ApiErrorPlaceholder {}
