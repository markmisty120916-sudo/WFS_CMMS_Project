/**
 * AIMI Engine — Aimi Pipeline Router
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface AimiPipelineRouterErrorPlaceholder {
  placeholder?: unknown;
}

export class AimiPipelineRouterError {}
