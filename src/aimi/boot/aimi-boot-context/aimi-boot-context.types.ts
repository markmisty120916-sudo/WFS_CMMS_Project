/**
 * AIMI Engine — Aimi Boot Context
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface AimiBootContextTypesPlaceholder {
  placeholder?: unknown;
}

export class AimiBootContextTypes {}
