/**
 * AIMI Engine — Aimi Activation Boot Context
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface AimiActivationBootContextTypesPlaceholder {
  placeholder?: unknown;
}

export class AimiActivationBootContextTypes {}
