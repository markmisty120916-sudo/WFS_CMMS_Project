/**
 * AIMI Engine — Telematics Routing
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum TELEMATICS_ROUTING_ERROR {}

export interface TelematicsRoutingError {
  placeholder?: unknown;
}

export class TelematicsRoutingErrorPlaceholder {}
