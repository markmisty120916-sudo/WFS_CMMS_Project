/**
 * AIMI Engine — Messaging
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum MESSAGING_ERROR {}

export interface MessagingError {
  placeholder?: unknown;
}

export class MessagingErrorPlaceholder {}
