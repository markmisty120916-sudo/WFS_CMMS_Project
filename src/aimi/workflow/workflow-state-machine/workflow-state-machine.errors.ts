/**
 * AIMI Engine — Workflow State Machine
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface WorkflowStateMachineErrorPlaceholder {
  placeholder?: unknown;
}

export class WorkflowStateMachineError {}
