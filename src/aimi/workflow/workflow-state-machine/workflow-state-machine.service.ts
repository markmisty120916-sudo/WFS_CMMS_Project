/**
 * AIMI Engine — Workflow State Machine
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface WorkflowStateMachineServicePlaceholder {
  placeholder?: unknown;
}

export class WorkflowStateMachineService {}
