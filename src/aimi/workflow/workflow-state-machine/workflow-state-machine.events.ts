/**
 * AIMI Engine — Workflow State Machine
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface WorkflowStateMachineEventsPlaceholder {
  placeholder?: unknown;
}

export class WorkflowStateMachineEvents {}
