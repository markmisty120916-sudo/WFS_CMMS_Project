/**
 * AIMI Engine — Workflow State Machine
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface WorkflowStateMachineSchemaPlaceholder {
  placeholder?: unknown;
}

export class WorkflowStateMachineSchema {}
