/**
 * AIMI Intelligence Interpretation — Event Normalization
 * Phase 100 — Event Normalization
 * WFS Universal CMMS
 * Empty logic stub. No business logic.
 */

export enum EVENT_NORMALIZATION_ERROR {}

export interface EventNormalizationError {
  placeholder?: unknown;
}

export class EventNormalizationErrorPlaceholder {}
