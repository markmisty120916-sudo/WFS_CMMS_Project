/**
 * AIMI Engine — Shop
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum SHOP_TYPE {}

export interface ShopCreateRequest {
  placeholder?: unknown;
}

export interface ShopUpdateRequest {
  placeholder?: unknown;
}

export interface ShopQuery {
  placeholder?: unknown;
}

export interface ShopResult {
  placeholder?: unknown;
}

export class ShopTypesPlaceholder {}
