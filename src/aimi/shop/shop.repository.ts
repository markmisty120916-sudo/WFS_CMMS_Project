/**
 * AIMI Engine — Shop
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum SHOP_REPOSITORY {}

export interface ShopRepositoryPlaceholder {
  placeholder?: unknown;
}

export class ShopRepository {}
