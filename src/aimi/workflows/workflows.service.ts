/**
 * AIMI Engine — Workflows
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum WORKFLOWS_SERVICE {}

export interface WorkflowsServicePlaceholder {
  placeholder?: unknown;
}

export class WorkflowsService {}
