/**
 * AIMI Engine — Workflows
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum WORKFLOWS_ERROR {}

export interface WorkflowsError {
  placeholder?: unknown;
}

export class WorkflowsErrorPlaceholder {}
