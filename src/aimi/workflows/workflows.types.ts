/**
 * AIMI Engine — Workflows
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum WORKFLOWS_TYPE {}

export interface WorkflowsCreateRequest {
  placeholder?: unknown;
}

export interface WorkflowsUpdateRequest {
  placeholder?: unknown;
}

export interface WorkflowsQuery {
  placeholder?: unknown;
}

export interface WorkflowsResult {
  placeholder?: unknown;
}

export class WorkflowsTypesPlaceholder {}
