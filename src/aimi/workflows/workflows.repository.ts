/**
 * AIMI Engine — Workflows
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum WORKFLOWS_REPOSITORY {}

export interface WorkflowsRepositoryPlaceholder {
  placeholder?: unknown;
}

export class WorkflowsRepository {}
