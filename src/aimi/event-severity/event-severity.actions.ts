/**
 * AIMI Engine — Event Severity
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

import {
  EventSeverityAggregationResult,
  EventSeverityClassificationResult,
} from "./event-severity.types";

export class EventSeverityActions {
  async handleAggregationResult(result: EventSeverityAggregationResult): Promise<void> {
    // scaffold only
  }

  async handleClassificationResult(result: EventSeverityClassificationResult): Promise<void> {
    // scaffold only
  }
}
