/**
 * AIMI Engine — Event Severity
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

import { EventSeverity } from "./event-severity.types";

export class EventSeverityValidators {
  static validateSeverity(severity: EventSeverity): void {
    // scaffold only
  }
}
