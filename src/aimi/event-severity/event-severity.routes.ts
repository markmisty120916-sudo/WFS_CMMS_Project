/**
 * AIMI Engine — Event Severity
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

import { EventSeverityController } from "./event-severity.controller";

export class EventSeverityRoutes {
  constructor(private readonly controller: EventSeverityController) {}

  registerRoutes(): void {
    // scaffold only
  }
}
