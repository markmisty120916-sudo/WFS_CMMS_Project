/**
 * AIMI Engine — Event Severity
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum EventSeverityDomainEvent {
  SeverityAggregated = "event_severity.severity_aggregated",
  SeverityClassified = "event_severity.severity_classified",
}
