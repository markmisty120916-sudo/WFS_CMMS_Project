/**
 * AIMI Engine — Security
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum SECURITY_ERROR {}

export interface SecurityError {
  placeholder?: unknown;
}

export class SecurityErrorPlaceholder {}
