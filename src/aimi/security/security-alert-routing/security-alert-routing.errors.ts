/**
 * AIMI Engine — Security Alert Routing
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface SecurityAlertRoutingErrorPlaceholder {
  placeholder?: unknown;
}

export class SecurityAlertRoutingError {}
