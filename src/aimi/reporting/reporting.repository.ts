/**
 * AIMI Engine — Reporting
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum REPORTING_REPOSITORY {}

export interface ReportingRepositoryPlaceholder {
  placeholder?: unknown;
}

export class ReportingRepository {}
