/**
 * AIMI Engine — Reporting
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum REPORTING_TYPE {}

export interface ReportingCreateRequest {
  placeholder?: unknown;
}

export interface ReportingUpdateRequest {
  placeholder?: unknown;
}

export interface ReportingQuery {
  placeholder?: unknown;
}

export interface ReportingResult {
  placeholder?: unknown;
}

export class ReportingTypesPlaceholder {}
