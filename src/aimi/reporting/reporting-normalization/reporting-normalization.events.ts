/**
 * AIMI Engine — Reporting Normalization
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ReportingNormalizationEventsPlaceholder {
  placeholder?: unknown;
}

export class ReportingNormalizationEvents {}
