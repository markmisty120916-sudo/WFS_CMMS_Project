/**
 * AIMI Engine — Reporting Aggregation
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ReportingAggregationEventsPlaceholder {
  placeholder?: unknown;
}

export class ReportingAggregationEvents {}
