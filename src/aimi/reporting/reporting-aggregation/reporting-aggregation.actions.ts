/**
 * AIMI Engine — Reporting Aggregation
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ReportingAggregationActionsPlaceholder {
  placeholder?: unknown;
}

export class ReportingAggregationActions {}
