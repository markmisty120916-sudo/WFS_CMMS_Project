/**
 * AIMI Engine — Reporting Aggregation
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ReportingAggregationSchemaPlaceholder {
  placeholder?: unknown;
}

export class ReportingAggregationSchema {}
