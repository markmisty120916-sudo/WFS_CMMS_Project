/**
 * AIMI Engine — Reporting Aggregation
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export interface ReportingAggregationTypesPlaceholder {
  placeholder?: unknown;
}

export class ReportingAggregationTypes {}
