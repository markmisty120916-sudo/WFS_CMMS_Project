/**
 * AIMI Engine — Reporting
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum REPORTING_ERROR {}

export interface ReportingError {
  placeholder?: unknown;
}

export class ReportingErrorPlaceholder {}
