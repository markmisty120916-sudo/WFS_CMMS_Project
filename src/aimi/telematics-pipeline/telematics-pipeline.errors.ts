/**
 * AIMI Engine — Telematics Pipeline
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum TELEMATICS_PIPELINE_ERROR {}

export interface TelematicsPipelineError {
  placeholder?: unknown;
}

export class TelematicsPipelineErrorPlaceholder {}
