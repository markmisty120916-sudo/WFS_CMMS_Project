/**
 * AIMI Engine — Scheduler
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum SCHEDULER_ACTION {}

export interface SchedulerAction {
  placeholder?: unknown;
}

export class SchedulerActionsPlaceholder {}

export const schedulerActions = {};
