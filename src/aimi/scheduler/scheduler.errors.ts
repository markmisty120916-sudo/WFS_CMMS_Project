/**
 * AIMI Engine — Scheduler
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum SCHEDULER_ERROR {}

export interface SchedulerError {
  placeholder?: unknown;
}

export class SchedulerErrorPlaceholder {}
