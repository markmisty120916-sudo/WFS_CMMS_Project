/**
 * AIMI Engine — Scheduler
 * WFS Universal CMMS
 * Scaffold only. No business logic.
 */

export enum SCHEDULER_TYPE {}

export interface SchedulerCreateRequest {
  placeholder?: unknown;
}

export interface SchedulerUpdateRequest {
  placeholder?: unknown;
}

export interface SchedulerQuery {
  placeholder?: unknown;
}

export interface SchedulerResult {
  placeholder?: unknown;
}

export class SchedulerTypesPlaceholder {}
