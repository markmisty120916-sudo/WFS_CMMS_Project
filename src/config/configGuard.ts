import type { ConfigPack } from "./configTypes";

export function requireFeature(config: ConfigPack, featureKey: string): void {
  void config;
  void featureKey;
  throw new Error("Not implemented");
}
